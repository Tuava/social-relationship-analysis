# Security policy

## Scope

本项目默认面向本地或受控私有环境。它会保存聊天、空间动态、关系事件、媒体和原始抓包，这些数据可能包含个人信息和认证上下文。

## 公开问题前

请不要在 issue 或公开讨论中附加：

- `backend/.env`、数据库 dump 或日志；
- QQ/QZone/NapCat Cookie、Token、JWT secret 或私钥；
- 未脱敏的聊天、空间、媒体、人物画像或导出报告。

## 报告漏洞

如果问题涉及凭证泄露、越权读取、远程执行、危险操作或敏感数据暴露，请先通过私下渠道联系维护者，不要立即公开复现数据。报告应包含影响范围、复现步骤和修复建议，但使用最小化、完全脱敏的样本。

## 部署原则

- 默认只绑定 `127.0.0.1`；
- MCP 默认使用本地 stdio，不应直接暴露公网；
- 采集仅限使用者有权访问的数据；
- 破坏性卸载和数据库删除必须显式确认；
- 生产环境由部署者负责 TLS、认证、网络隔离、备份加密和权限管理。
